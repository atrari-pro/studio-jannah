# Supabase — mêmes valeurs que apps/web/.env (Project Settings → API)
# Utilisé uniquement par l'admin (/admin) — le wizard démo public n'en a pas besoin.
PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your_anon_or_publishable_key
